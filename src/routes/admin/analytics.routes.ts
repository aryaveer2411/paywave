import { Router } from 'express';
// import { totalSalesByMerchants, salesByMerchant, totalActivePlans } from '../../controllers/admin.controller';

const analyticsRouter = Router();

// analyticsRouter.get('/total-sales', totalSalesByMerchants);
// analyticsRouter.get('/sales/:merchantId', salesByMerchant);
// analyticsRouter.get('/active-plans', totalActivePlans);

export default analyticsRouter;
